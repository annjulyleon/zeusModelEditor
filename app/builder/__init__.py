"""Build model files and building lines from the json.

Modules exported by these packages:

- `file_builder`: contains operations to build model file
- `line_builder`: contains operations to build single model line from json
"""